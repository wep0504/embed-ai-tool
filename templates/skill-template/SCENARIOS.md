# 场景模板

新增或修改 skill 时，可直接复用这个文件定义场景覆盖。

## 正常路径

- 工作区：
- 用户意图：
- 必要输入：
- 自动探测优先级：`显式输入 > 工作区线索 > 历史上下文 > 默认值`
- 预期状态：`success | partial`
- 预期输出：
  - `status`:
  - `summary`:
  - `evidence`:
  - `next_actions`:
- 预期交接：

## 缺失依赖

- 缺失工具或运行时：
- 探测证据：
- 预期失败分类：`environment-missing`
- 预期重试策略（若可恢复）：
  - 最大重试次数：
  - 重试间隔：
  - 超限后状态：`blocked | failed`
- 预期下一步：

## 上下文歧义

- 存在歧义的对象：
- 候选列表：
- 预期状态：`blocked`
- 预期失败分类：`ambiguous-context`
- 停止猜测条件：任意选择一个候选都会导致时间浪费或流程风险
- 预期用户补充信息：

## 技能专属边界场景

- 边界场景：
- 证据：
- 预期结果（建议使用统一输出结构）：
  - `status`:
  - `summary`:
  - `evidence`:
  - `next_actions`:
- 推荐下一步技能：
